<div class="">
    <h2 class="mt-0 mb-2">4
        <span class="inline-block text-sm">Results found containing digital in All categories</span></h2>
    <div>
        {% from 'list-item.njk' import listItem as listItem with context %}
        {% block content %}
            {% for item in items %}
                {{listItem(item) }}
            {% endfor %}
        {% endblock %}